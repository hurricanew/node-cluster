{
  "name": "node-sandbox",
  "version": "1.0.0",
  "description": "Simple Node Sandbox",
  "main": "src/cluster.js",
  "scripts": {
    "start": "nodemon src/cluster.js localhost 8080",
    "loadtest": "loadtest -c 10 --rps 10 -t 10 http://localhost:8080"
  },
  "dependencies": {
    "loadtest": "3.0.7"
  },
  "devDependencies": {
    "nodemon": "1.18.4"
  }
}